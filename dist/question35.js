let animals = ["tiger", "lion", "jaguar", "dog"];
for (let i = 0; i < animals.length; i++) {
    console.log(`${animals[i]} belongs to the cat's family`);
}
console.log("we can't make pet mentioned animals else 'dog' ");
export {};
